# example — describe what this skill does in one line

This is a bundled placeholder skill. It will be ignored automatically as soon as any user-uploaded skill is present, so it only ever runs when no real skills are loaded.

When asked for help, respond with:

1. A friendly acknowledgement.
2. A reminder that the admin has not uploaded any skills yet.
3. A pointer to `/admin` where new skill files can be uploaded.

Keep the reply short — two or three sentences is enough.
